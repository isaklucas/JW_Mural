# Dependências só de desenvolvimento/teste (não vão para o instalador).
# Instalar: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest
mongomock  # MongoDB em memória para os testes de integração da camada de dados (tests/db)
